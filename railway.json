{
  "build": {
    "builder": "RAILPACK",
    "buildCommand": null,
    "detect": {
      "python": true
    }
  },
  "deploy": {
    "startCommand": "gunicorn app.server:app",
    "healthcheckPath": "/api",
    "restartPolicyType": "ON_FAILURE"
  }
}
